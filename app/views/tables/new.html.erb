<div class="container mt-4">
  <h1>Adicionar Mesa</h1>

  <%= form_with(model: @table, local: true) do |form| %>
    <div class="mb-3">
      <%= form.label :number,"Número", class: "form-label" %>
      <%= form.number_field :number, class: "form-control" %>
    </div>

    <%= form.submit "Criar Mesa", class: "btn btn-primary" %>
  <% end %>
</div>
